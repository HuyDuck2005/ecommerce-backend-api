version: "3.8"
services:
  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: suser
      POSTGRES_PASSWORD: "123456"
      POSTGRES_DB: mydb
    ports:
      - "54232:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U suser -d mydb"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - db_data:/var/lib/postgresql/data
volumes:
  db_data: